tags-input([(ngModel)]="boundaryFile.tags")

div
    p
        input(type="text",name="title",placeholder="Title of boundary file",[(ngModel)]="boundaryFile.title")
    p
        input(type="file",name="boundaryFile", accept='.json', (change)="enableUploadButton()")#boundaryFile
br
div
    button#upload_button((click)="uploadSelectedFile()", disabled='true')
        | Upload
    div#container
        div#progressbarContainer
            div#progressbar
        div#status
